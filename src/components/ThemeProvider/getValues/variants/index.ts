import { default as getColors } from "./getVariantColors";
import { default as getStyles } from "./getVariantStyles";

const variants = {
  getStyles: getStyles,
  getColors: getColors,
};

export default variants;
