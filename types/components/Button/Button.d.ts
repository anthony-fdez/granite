import { IButtonProps } from "./Button.types";
declare const Button: ({ children, variant, color, padding, margin, borderRadius, loading, fontColor, width, spinnerVariant, align, ...args }: IButtonProps) => import("@emotion/react/jsx-runtime").JSX.Element;
export default Button;
