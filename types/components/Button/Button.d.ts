import { IButtonProps } from "./Button.types";
declare const Button: ({ children, variant, color, padding, margin, borderRadius, loading, fontColor, spinnerVariant, align, disabled, fullWidth, backgroundColor, ...args }: IButtonProps) => import("@emotion/react/jsx-runtime").JSX.Element;
export default Button;
